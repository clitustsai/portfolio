{
  "name": "clitus-portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "install-deps": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
